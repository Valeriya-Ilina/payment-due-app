<nav>
  <div class="nav-wrapper teal lighten-2">
    <% if (currentUser) { %>
      <a href="/bills" class="">HOME</a>
    <% } else { %>
      <span class="left-align"> Payment Due App - track your obligations </span>
    <% } %>
    <ul class="right">
        <% if (currentUser) { %>
            <li>
              Welcome, <%= currentUser.username %>
            </li>
            <li id="logout-btn">
                <form action="/sessions?_method=DELETE" method="POST">
                    <button class="btn">SIGN OUT</button>
                </form>
            </li>
        <% } else { %>
            <li>
              <a href="/users/signup">Sign Up</a>
            </li>
            <li>
              <a href="/">Log In</a>
            </li>
        <% } %>
    </ul>
  </div>
</nav>
